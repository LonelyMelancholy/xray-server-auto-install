# .local for start telegram and iptables action
# done work
# done test
[sshd]
enabled = true
port = {PORT}
filter = sshd
backend = systemd
maxretry = 3
bantime = 30d
findtime = 30d
action = iptables-allports[name=SSH, protocol=all, blocktype=DROP] ssh_telegram
usedns = no