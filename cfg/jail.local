# .local f2b conf for start telegram and iptables action
# ip6 and ip4 ban, any protocol, any port - drop
[sshd]
enabled = true
port = {PORT}
filter = sshd
backend = systemd
maxretry = 3
bantime = 30d
findtime = 30d
usedns = no
action = nftables[type=custom, name=SSH, blocktype=drop] ssh_telegram