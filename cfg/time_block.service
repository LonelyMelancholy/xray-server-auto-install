[Unit]
Description=Service for running a script that blocks expired user in xray, start via timer
Requires=precreate_lockfiles.service
After=precreate_lockfiles.service

[Service]
Type=oneshot
User=telegram_gateway
ExecStart=/usr/local/bin/service/time_block.sh
